# Frontend (React + Vite)

## Setup

```powershell
npm install
```

## Run (dev)

```powershell
npm run dev
```

## Backend base URL

By default the frontend calls `http://localhost:8000`.

To override, create `mi-platform/.env.local`:

```env
VITE_API_BASE_URL=http://localhost:8000
```
